---
description: Guidelines for generating automated Playwright tests using Playwright MCP. Use when keywords like "generate test" mentioned.
alwaysApply: false
---
  Dynamically generates Playwright MCP UI tests.
  Parameters: payer, numTests, scope.

parameters:
  payer: { type: string }
  numTests: { type: int, default: 10 }
  scope: { type: string, default: "core user flows" }

login:
  url: https://tipalti-login.dev.tipalti-dev.com/login
  email: automationmain@mailinator.com
  password: Tipalti123

prompt: |
  Use Playwright MCP.
  1. Log in at {{login.url}} with provided creds.
  2. Switch to payer {{payer}}.
  3. Explore flows described by "{{scope}}" starting at https://tipalti-login.dev.tipalti-dev.com/login/hub/password?isSsoFlow=false&username=payees1automation@gmail.com&redirectSsoUrl.
  4. Generate {{numTests}} POM‑style tests with clear selectors and proper waits.
  5. Save to tests/ui/{{payer}}_generated.spec.ts in TypeScript.